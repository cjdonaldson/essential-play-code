GET  /             controllers.LandingController.index

GET  /chat/:sessionId         controllers.ChatController.index(sessionId: String)
POST /chat/:sessionId         controllers.ChatController.submitMessage(sessionId: String)

GET  /login        controllers.AuthController.login
POST /login        controllers.AuthController.submitLogin

GET  /assets/*file controllers.Assets.at(path="/public", file)
